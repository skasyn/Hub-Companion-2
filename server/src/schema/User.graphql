type User {
    id: ID!
    name: String!
    email: String!
    year: Int
    plan: Int
    privilege: Int
    activities: [UserPresence!]!
    notifications: [UserMessages!]!
}

type UserMessages {
    id: ID!
    seen: Boolean!
    author: String!
    date: DateTime!
    message: String!
}
