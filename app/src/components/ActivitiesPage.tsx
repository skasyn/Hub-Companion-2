import React from "react";
import {
  Container,
  Typography
} from "@material-ui/core";

export const ActivitiesPage: React.FC = () => {
  return (
    <Container>
      <Typography>
        Activities
      </Typography>
    </Container>
  );
};