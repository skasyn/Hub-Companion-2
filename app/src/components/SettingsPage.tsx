import React from "react";
import {
  Container,
  Typography
} from "@material-ui/core";

export const SettingsPage: React.FC = () => {
  return (
    <Container>
      <Typography>
        Settings
      </Typography>
    </Container>
  );
};